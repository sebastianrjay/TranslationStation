'use strict';

describe('translationStation.input module', function() {

  beforeEach(module('translationStation.input'));

  describe('input controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var inputCtrl = $controller('InputCtrl');
      expect(inputCtrl).toBeDefined();
    }));

  });
});