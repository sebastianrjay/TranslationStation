'use strict';

describe('translationStation.frengly-results module', function() {

  beforeEach(module('translationStation.frengly-results'));

  describe('frengly-results controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var frengly-resultsCtrl = $controller('FrenglyResultsCtrl');
      expect(frengly-resultsCtrl).toBeDefined();
    }));

  });
});